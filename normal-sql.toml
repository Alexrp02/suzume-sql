# normal-sql connection config.
#
# Run with:   normal-sql                              (uses ./normal-sql.toml; shows a picker)
#             normal-sql local                         (open the connection named "local")
#             normal-sql -c path.toml                  (use a different config file)
#             normal-sql postgresql://u:p@host/db      (connect directly via a connection string)
#             normal-sql sqlite://./demo.db            (sqlite needs the sqlite:// prefix)

[[connections]]
name = "local"
engine = "sqlite"
path = "./demo.db"

# Example PostgreSQL connection (uncomment and adjust):
# [[connections]]
# name = "prod"
# engine = "postgres"
# url = "postgresql://user:password@localhost:5432/app"
